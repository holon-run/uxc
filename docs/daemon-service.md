# Run UXC Daemon as a Managed Service

This public document has moved to the docs site:

- https://uxc.holon.run/daemon/service/

The repository-local `docs/` copy is now kept only as a compatibility pointer.
